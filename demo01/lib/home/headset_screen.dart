import 'package:flutter/material.dart';

class HeadSetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Headset'),),
      body: Center(child: Text('HeadSetBody'),),
    );
  }
}